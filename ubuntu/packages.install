#!/bin/bash

BASE_DIR=`cd "$(dirname "$0")"; pwd`

f=$BASE_DIR/packages.config; [ -f "$f" ] || f=$f.sample
PACKAGES=$(cat "$f")

sudo apt-get update

for package in ${PACKAGES[*]}
do
  sudo apt-get install -y $package
done
